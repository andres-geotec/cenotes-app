<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geotec.cenotesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MainActivity"
            android:theme="@style/AtNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.EditorCenoteActivity"
            android:parentActivityName=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.GeneralidadesActivity"
            android:parentActivityName=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.ClasificacionActivity"></activity>
        <activity android:name=".ui.activities.UsoActivity" />
        <activity android:name=".ui.activities.MorfometriaActivity" />
        <activity android:name=".ui.activities.ProblematicaActivity" />
        <activity android:name=".ui.activities.GestionActivity" />
        <activity android:name=".ui.activities.FotografiasActivity" />
    </application>

</manifest>